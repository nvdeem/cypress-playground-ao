import { visit } from '../elements/pages/LoginPage';

//-PAGE-//
//-TESTS-//

console.log(LoginPage);

describe('Log in page tests', () => {
	it('should error when end user inputs an unrecognised email and password', () => {
		LoginPage;
	});
});
